# =============================================================================
#  requirements.txt  —  CLOUD DEPLOYMENT
#  Used by: Render, Hugging Face Spaces (read automatically from repo root)
#
#  Key differences from requirements-local.txt:
#    opencv-python-headless  instead of  opencv-contrib-python  (no display)
#    tensorflow only         instead of  tensorflow + tensorflow-intel (Linux)
#    fastapi + uvicorn added (HTTP predict endpoint — replaces streamlit-webrtc)
#    pydantic added          (FastAPI request body model)
#    streamlit-webrtc removed  (replaced by browser JS + HTTP fetch in v8.0)
#    aiortc removed            (no longer needed — no WebRTC)
#    av removed                (no longer needed — no av.VideoFrame decoding)
#    pyttsx3 removed         (no audio drivers on cloud — Web Speech API used)
#    sounddevice removed     (no audio hardware on cloud)
#    kaggle / kagglesdk removed  (dataset tools, not needed at runtime)
#    tensorflow-io-gcs-filesystem removed  (GCS only, not needed here)
# =============================================================================

# ── Core ML ──────────────────────────────────────────────────────────────────
tensorflow==2.15.0
keras==2.15.0

# ── Computer Vision ──────────────────────────────────────────────────────────
mediapipe==0.10.9

# headless build: identical API to opencv-contrib-python for all operations
# used in this project; omits GUI/display dependencies that crash on servers
opencv-python-headless==4.9.0.80

numpy==1.26.4

# ── Streamlit ────────────────────────────────────────────────────────────────
streamlit>=1.32.0

# Browser JS captures webcam frames and POSTs base64 JPEGs to /predict.
# FastAPI runs in a daemon thread on port 8000 alongside Streamlit.

# ── Data / utilities ─────────────────────────────────────────────────────────
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
pandas==3.0.0
pillow==12.1.1
requests==2.32.5

# ── Pinned transitive deps (for reproducible builds) ─────────────────────────
absl-py==2.4.0
astunparse==1.6.3
flatbuffers==25.12.19
gast==0.7.0
google-pasta==0.2.0
grpcio==1.78.0
h5py==3.15.1
ml-dtypes==0.2.0
opt_einsum==3.4.0
protobuf==3.20.3
tensorboard==2.15.2
tensorboard-data-server==0.7.2
tensorflow-estimator==2.15.0
termcolor==3.3.0
wrapt==1.14.2