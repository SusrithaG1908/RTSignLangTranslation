# =============================================================================
#  requirements.txt  —  CLOUD DEPLOYMENT
#  Used by: Hugging Face Spaces (Docker SDK) — installed inside the container
#
#  Key differences from requirements-local.txt:
#    opencv-python-headless  instead of  opencv-contrib-python  (no display)
#    tensorflow only         instead of  tensorflow + tensorflow-intel (Linux)
#    pyttsx3 removed         (no audio drivers on cloud — Web Speech API used)
#    sounddevice removed     (no audio hardware on cloud)
#    kaggle / kagglesdk removed  (dataset tools, not needed at runtime)
#    tensorflow-io-gcs-filesystem removed  (GCS only, not needed here)
#    streamlit-webrtc / aiortc / av removed  (replaced by browser JS + HTTP fetch)
#    fastapi / uvicorn / pydantic NOT needed  (predict server uses stdlib http.server)
#    scikit-learn / matplotlib / seaborn / pandas removed  (training only, not runtime)
# =============================================================================

# ── Core ML ──────────────────────────────────────────────────────────────────
tensorflow==2.15.0
keras==2.15.0

# ── Computer Vision ──────────────────────────────────────────────────────────
mediapipe==0.10.9

# headless build — no GUI deps, identical API for all operations used at runtime
opencv-python-headless==4.9.0.80

numpy==1.26.4

# ── Streamlit ────────────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Utilities ────────────────────────────────────────────────────────────────
pillow==12.1.1
requests==2.32.5

# ── Pinned transitive deps (for reproducible builds) ─────────────────────────
absl-py==2.4.0
astunparse==1.6.3
flatbuffers==25.12.19
gast==0.7.0
google-pasta==0.2.0
grpcio==1.78.0
h5py==3.15.1
ml-dtypes==0.2.0
opt_einsum==3.4.0
protobuf==3.20.3
tensorboard==2.15.2
tensorboard-data-server==0.7.2
tensorflow-estimator==2.15.0
termcolor==3.3.0
wrapt==1.14.2